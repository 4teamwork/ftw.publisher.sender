Introduction
============

The package ftw.publisher.sender is used for sending objects to a instance with a installed ftw.publisher.receiever package. It provides the configlet for configuring the publisher product.
